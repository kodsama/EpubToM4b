# Core (always needed)
EbookLib>=0.18
beautifulsoup4>=4.12
lxml>=5.0
soundfile>=0.12
numpy>=1.24
tqdm>=4.66

# Backend extras — install only the one you use:
# kokoro (local, default):
#   kokoro>=0.9
#   misaki[fr]
# openai (cloud):
#   openai>=1.40
# elevenlabs (cloud):
#   elevenlabs>=1.5
# piper (local): install the `piper` binary + a fr_FR-*.onnx voice separately
#
# System packages (not pip): ffmpeg, espeak-ng
